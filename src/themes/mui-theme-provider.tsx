import { createTheme } from "@mui/material";

export const defaultMUITheme = createTheme(
    {
        typography: {
          fontFamily: [
            'Poppins',
          ].join(','),
        },}
)